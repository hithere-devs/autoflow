export const aiNode = {
	data: {
		// can come from two places, either from some node or written directly in the node
		// if it is coming from some node, it will be a reference to that node
		// if it is written directly in the node, it will be a string we will be getting from the database
	},
	model: '',
	system: '',
	prompt: '',
	result: '',
};
